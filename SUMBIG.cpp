#include <bits/stdc++.h>

using namespace std;

int main()
{
    string a, b;
    cin>>a>>b;
    string sum;
    int temp = 0
    if(a.length()>=b.length()){
        sum = a;
    }else{
        sum =b;
    }
    //chay den b.length +1 vi con xet du o so cuoi cung
    for(int i; i<=b.length();i++){

    }
    
    cout<<sum;
    cout<<endl;
    return 0;
}