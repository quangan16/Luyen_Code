#include <iostream>
#include <math.h>
#include<string>

using namespace std;

int main(){
	int a, b, c;
	cin >> a >> b >> c;
	cout << (a + b + c) / 3 << endl;
	cout << sqrt(a * b * c) << endl;
	getchar();
	return 0;
}